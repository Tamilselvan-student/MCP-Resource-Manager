/**
 * Generates a random ID
 *
 * Note: May not return a valid value on older browsers - we're fine with this for now
 */
export declare function generateRandomId(): string | undefined;
export declare function generateRandomIdWithNonUniqueFallback(): string;
